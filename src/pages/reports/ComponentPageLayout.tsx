import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const ReportPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default ReportPageLayout;